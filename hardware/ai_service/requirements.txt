fastapi==0.111.0
uvicorn[standard]==0.29.0
opencv-python>=4.10.0       # 4.10+ required for numpy 2.x compatibility
numpy>=2.0.0
httpx==0.27.0
python-dotenv==1.0.1

# ─── AI Models ───────────────────────────────────────────────────────────────
ultralytics>=8.2.0          # YOLOv8 (plate_detector.pt + face_detector.pt)
easyocr>=1.7.1              # OCR đọc biển số
insightface>=0.7.3          # Face embedding (buffalo_sc, tự download)
onnxruntime>=1.18.0         # Runtime cho InsightFace ONNX models
paddlepaddle>=3.3.0         # PaddleOCR backend
paddleocr>=3.6.0            # OCR biển số (dùng PaddleOCR 3.x API)
paddlex[ocr-core]>=3.6.0    # PaddleX OCR pipeline deps
opencv-contrib-python>=4.10.0  # Required by paddlex[ocr-core]
